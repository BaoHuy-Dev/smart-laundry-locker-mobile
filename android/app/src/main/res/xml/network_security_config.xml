<?xml version="1.0" encoding="utf-8"?>
<!--
  Cho phép HTTP cleartext CHỈ tới backend đã deploy (DigitalOcean droplet) và các
  host dùng khi chạy backend local. Phần còn lại của app giữ mặc định an toàn
  (chỉ HTTPS). Cần thiết vì gateway đang phục vụ qua http:// (chưa có TLS), trong
  khi Android API 28+ chặn cleartext mặc định.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">146.190.84.136</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
